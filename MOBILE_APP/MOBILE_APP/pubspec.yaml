name: resume_ai_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  google_fonts: ^6.2.0
  file_picker: ^8.0.0
  syncfusion_flutter_pdf: ^25.1.37 
  cupertino_icons: ^1.0.8
  google_generative_ai: ^0.4.7
  

dependency_overrides:
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true